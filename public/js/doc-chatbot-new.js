/**
 * Script pour le chatbot de documentation - DÉSACTIVÉ à la demande de l'utilisateur
 * Ce fichier est conservé pour compatibilité mais n'initialise plus le chatbot.
 */

document.addEventListener('DOMContentLoaded', function() {
  console.log("🤖 Le chatbot de documentation a été désactivé");
  
  // Le chatbot a été complètement supprimé de la page de documentation
  // Ce script reste pour compatibilité mais n'effectue aucune action
  
  // Aucune initialisation ou configuration n'est effectuée
  
  console.log("Chatbot de documentation désactivé à la demande de l'utilisateur");
});